# Tasks

- [x] 1. Capture a deterministic tiny-chunk burst failure before production changes.
- [x] 2. Backport upstream byte accounting and both helper queue allocations.
- [x] 3. Verify byte/event limits, dequeue, cancellation, generation isolation, malformed frames and separate WebSocket message capacity.
- [x] 4. Run related unit tests, local helper surface, Ruff, ty, architecture, packaging and strict scoped spec checks; sync and archive.
